# Example: Task mode agent with AgentTool bindings, context, and scheduling
apiVersion: agents.agentops.io/v1alpha1
kind: Agent
metadata:
  name: scheduled-oci-agent
  labels:
    app.kubernetes.io/name: agentops-core
    app.kubernetes.io/managed-by: kustomize
spec:
  mode: task
  model: openai/gpt-4.1-mini

  image: ghcr.io/samyn92/agent-runtime-fantasy:latest
  imagePullPolicy: IfNotPresent
  builtinTools: []

  providers:
    - name: openai
      apiKeySecret:
        name: llm-api-keys
        key: OPENAI_API_KEY

  systemPrompt: |
    You triage issues, propose next actions, and keep outputs concise.

  contextFiles:
    - configMapRef:
        name: agent-context
        key: AGENTS.md

  # Tool bindings reference AgentTool CRs by name
  tools:
    - name: jira-tools
    - name: repo-helpers

  env:
    WORKSPACE_ROOT: /workspace
    FEATURE_FLAGS: issue-triage

  secrets:
    - name: GITHUB_TOKEN
      secretRef:
        name: scm-credentials
        key: github-token

  schedule: "0 * * * *"
  schedulePrompt: |
    Check newly opened incidents and propose next actions.

  toolHooks:
    auditTools:
      - jira-tools

  networkPolicy:
    enabled: true

  resources:
    requests:
      memory: 128Mi
      cpu: 100m
    limits:
      memory: 256Mi

  timeout: "10m"
