---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: channels.agents.agentops.io
spec:
  group: agents.agentops.io
  names:
    kind: Channel
    listKind: ChannelList
    plural: channels
    shortNames:
    - ch
    singular: channel
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.agentRef
      name: Agent
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.webhookURL
      name: Webhook
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Channel is the Schema for the channels API.
          Universal external ingress. Bridges external platforms to Agents.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ChannelSpec defines the desired state of Channel.
            properties:
              agentRef:
                description: Name of the Agent CR to target.
                minLength: 1
                type: string
              discord:
                description: Discord bot configuration.
                properties:
                  allowedChannels:
                    description: Allowed Discord channel IDs.
                    items:
                      type: string
                    type: array
                  botTokenSecret:
                    description: Secret containing the bot token.
                    properties:
                      key:
                        description: Key within the Secret.
                        type: string
                      name:
                        description: Name of the Secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                required:
                - botTokenSecret
                type: object
              github:
                description: GitHub webhook configuration.
                properties:
                  actions:
                    description: Filter by action (e.g. "opened", "synchronize").
                    items:
                      type: string
                    type: array
                  events:
                    description: GitHub webhook events to listen for (e.g. "pull_request").
                    items:
                      type: string
                    type: array
                  labels:
                    description: Filter by labels on the object.
                    items:
                      type: string
                    type: array
                  secret:
                    description: Webhook secret for signature verification.
                    properties:
                      key:
                        description: Key within the Secret.
                        type: string
                      name:
                        description: Name of the Secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                required:
                - events
                - secret
                type: object
              gitlab:
                description: GitLab webhook configuration.
                properties:
                  actions:
                    description: Filter by action (e.g. "open").
                    items:
                      type: string
                    type: array
                  events:
                    description: GitLab webhook events to listen for (e.g. "Issue
                      Hook").
                    items:
                      type: string
                    type: array
                  labels:
                    description: Filter by labels on the object.
                    items:
                      type: string
                    type: array
                  secret:
                    description: Webhook secret for signature verification.
                    properties:
                      key:
                        description: Key within the Secret.
                        type: string
                      name:
                        description: Name of the Secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                required:
                - events
                - secret
                type: object
              image:
                description: Container image for the channel bridge.
                minLength: 1
                type: string
              imagePullPolicy:
                default: IfNotPresent
                description: Image pull policy.
                enum:
                - Always
                - IfNotPresent
                - Never
                type: string
              prompt:
                description: |-
                  Go text/template rendered with event data. Required for event types.
                  Chat types forward user messages directly.
                type: string
              replicas:
                default: 1
                description: Number of replicas for the channel bridge.
                format: int32
                minimum: 1
                type: integer
              resources:
                description: Compute resources for the channel container.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This is an alpha field and requires enabling the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              slack:
                description: Slack bot configuration.
                properties:
                  allowedChannels:
                    description: Allowed Slack channel IDs.
                    items:
                      type: string
                    type: array
                  botTokenSecret:
                    description: Secret containing the bot token.
                    properties:
                      key:
                        description: Key within the Secret.
                        type: string
                      name:
                        description: Name of the Secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                required:
                - botTokenSecret
                type: object
              telegram:
                description: Telegram bot configuration.
                properties:
                  allowedChats:
                    description: Allowed Telegram chat IDs.
                    items:
                      type: string
                    type: array
                  allowedUsers:
                    description: Allowed Telegram user IDs.
                    items:
                      type: string
                    type: array
                  botTokenSecret:
                    description: Secret containing the bot token.
                    properties:
                      key:
                        description: Key within the Secret.
                        type: string
                      name:
                        description: Name of the Secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                required:
                - botTokenSecret
                type: object
              type:
                description: 'Channel type: telegram, slack, discord, gitlab, github,
                  webhook.'
                enum:
                - telegram
                - slack
                - discord
                - gitlab
                - github
                - webhook
                type: string
              webhook:
                description: Webhook ingress configuration (host, TLS, etc.).
                properties:
                  host:
                    description: Hostname for the ingress.
                    type: string
                  ingressClassName:
                    description: Ingress class name.
                    type: string
                  path:
                    description: Path (defaults to /).
                    type: string
                  tls:
                    description: TLS configuration.
                    properties:
                      clusterIssuer:
                        description: Cert-manager cluster issuer name.
                        type: string
                    required:
                    - clusterIssuer
                    type: object
                required:
                - host
                type: object
              webhookConfig:
                description: Generic webhook configuration.
                properties:
                  secret:
                    description: Optional HMAC secret for signature verification.
                    properties:
                      key:
                        description: Key within the Secret.
                        type: string
                      name:
                        description: Name of the Secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                type: object
            required:
            - agentRef
            - image
            - type
            type: object
          status:
            description: ChannelStatus defines the observed state of Channel.
            properties:
              conditions:
                description: Standard conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              phase:
                description: 'Current phase: Pending, Ready, Failed.'
                type: string
              serviceURL:
                description: Internal service URL.
                type: string
              webhookURL:
                description: External webhook URL (if ingress configured).
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
